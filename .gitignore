# Real API key -- never commit this. Copy pipeline/config_local.example.py to
# pipeline/config_local.py and fill in your own key instead.
pipeline/config_local.py

# Weekly pipeline cache/output -- large, regenerated by running weekly_update.py, and
# already reflected in ChalkTalk.html's hardcoded arrays after each real run. No reason to
# version-control multi-MB parquet/json snapshots week to week.
pipeline/*.parquet
pipeline/_cache/
pipeline/week_odds.json
pipeline/wx_*.json
pipeline/schedule_2026_full.csv
pipeline/__pycache__/
odds_pull/

# OS/editor noise
.DS_Store
